apiVersion: v1
kind: ConfigMap
metadata:
  name: ourbox-landing
  namespace: ourbox-system
  labels:
    app.kubernetes.io/name: ourbox-landing
    app.kubernetes.io/part-of: ourbox-os
data:
  message: "OurBox platform contract bootstrap baseline"
