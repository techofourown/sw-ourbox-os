# Publish Record Helpers

This directory owns shared publish-record JSON generation for upstream artifact publish flows.

Use `write-publish-record.py` from artifact `publish.sh` scripts to emit canonical machine-readable records that conform to `schemas/artifact-publish-record.schema.json`.
