# Artifact Provenance

Platform contract artifacts include baseline manifests, platform configuration, and gateway/access-mode defaults.

Existing provenance workflow, tag policy, and verification logic are unchanged.
