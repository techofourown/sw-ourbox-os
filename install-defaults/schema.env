# Install defaults manifest schema for installer runtime remote config.
# This file is bundled into the install-defaults OCI artifact.

INSTALL_DEFAULTS_SCHEMA=1
INSTALL_DEFAULTS_KIND=install-defaults
