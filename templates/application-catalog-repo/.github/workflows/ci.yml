name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oras-project/setup-oras@v1
      - name: Validate script syntax
        run: bash -n scripts/render-catalog-bundle.sh scripts/check-catalog-bundle-smoke.sh scripts/check-image-refs-exist.sh
      - name: Validate and render catalog bundle
        run: bash scripts/check-catalog-bundle-smoke.sh
      - name: Validate referenced images exist
        run: bash scripts/check-image-refs-exist.sh
