{
  "name": "sw-ourbox-os-dataset",
  "private": true,
  "version": "0.0.0",
  "description": "OurBox OS GraphMD dataset + CI validation.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate:dataset": "node tools/requirements/validate-dataset.cjs",
    "build:specs": "node tools/requirements/compile-all-specs.cjs",
    "test": "npm run validate:dataset && npm run build:specs && npm run validate:schemas",
    "verify:spec-artifacts": "node tools/requirements/verify-spec-artifacts.cjs",
    "validate:schemas": "node tools/policy/validate-schemas.cjs"
  },
  "devDependencies": {
    "@graphmd/dataset": "<1.0.0",
    "@noble/hashes": "^1.8.0",
    "yaml": "^2.8.2"
  }
}
