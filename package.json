{
  "name": "sw-ourbox-os-dataset",
  "private": true,
  "version": "0.0.0",
  "description": "OurBox OS GraphMD dataset + CI validation.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate:dataset": "node tools/validate-dataset.cjs",
    "build:spec": "node tools/compile-syrs.cjs",
    "test": "npm run validate:dataset && npm run build:spec"
  },
  "devDependencies": {
    "@graphmd/dataset": "0.x",
    "@noble/hashes": "^1.8.0",
    "yaml": "^2.8.2"
  }
}
