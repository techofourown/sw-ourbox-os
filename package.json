{
  "name": "sw-ourbox-os-dataset",
  "private": true,
  "version": "0.0.0",
  "description": "OurBox OS GraphMD dataset + CI validation.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate:dataset": "node tools/validate-dataset.cjs",
    "build:syrs": "node tools/compile-syrs.cjs",
    "test": "npm run validate:dataset && npm run build:syrs"
  },
  "devDependencies": {
    "@graphmd/dataset": "<1.0.0",
    "@noble/hashes": "^1.8.0",
    "yaml": "^2.8.2"
  }
}
