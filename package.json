{
  "name": "sw-ourbox-os",
  "private": true,
  "version": "0.0.0",
  "description": "OurBox OS repo with GraphMD dataset validation in CI.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate:dataset": "node tools/validate-dataset.cjs",
    "build:spec": "node tools/compile-spec.cjs",
    "test": "npm run validate:dataset && npm run build:spec"
  },
  "devDependencies": {
    "@graphmd/dataset": "0.x"
  }
}
